# Dedicated hostname deployment. Use /mod/ only for an explicitly approved subpath deployment.
VITE_BASE_PATH=/

# Optional, deployment-owned ECharts-compatible GeoJSON FeatureCollection.
# MOD does not bundle a China map. Leave empty to show the honest map-unavailable state.
VITE_CHINA_MAP_GEOJSON_URL=
