# 强制执行规范（Enforcement Standard）

更新日期：2026-09-03
状态：现行
适用范围：所有在 `/home/ubuntu/mod` 工作的人类与 AI（Kiro、Claude、Codex、Antigravity/agy、Gemini 及后续接入者）

## 本文定位

本仓库已有完整的**内容型规范**：`AGENTS.md`、`CONTRIBUTING.md`、`docs/CURRENT-STATE.md` 与
`docs/development/` 下的各项标准，清楚说明了“应该做什么、不应该做什么”。

本文不重复这些内容。本文只回答它们没有回答的一个问题：

> 当这些规则被违反时，靠什么拦住？

现有规范是**须知**，本文是**闸门**。须知靠阅读与自觉，闸门在动作发生的那一刻自动生效。
本项目由 AI 驱动，没有资深工程师的隐性判断兜底，因此“靠自觉遵守”不构成防线。
凡是违反一次代价即不可逆的红线，必须有文档之外的机制强制；文档只负责让人和 AI 理解，
机制负责在理解错时拦住执行。

冲突时，权威顺序仍以 `AGENTS.md` 为准；本文不覆盖任何现有规则的内容，只补充其执行方式。

## 核心原则

1. **不依赖自觉**：任何红线不得仅以“文档里写了”作为防线。
2. **闸门在动作点**：拦截发生在动作即将执行的位置，与是哪个 AI、读没读文档、用户是否在场无关。
3. **默认安全**：默认只读、默认不连生产、默认不删除；提升权限必须显式且留痕。
4. **跨 CLI 等效**：同一条红线在所有 CLI 会话中都必须成立，不得只在某一个工具里生效。
5. **派生优于手写**：能从源码、Schema、数据库派生的事实（如数据行数）不得手写进文档。

## 已知机制盲区（依据 2026-09-03 复盘）

本项目曾有一套多 Agent 协作机制雏形（`.kiro/steering/` 与已归档的 `.kiro/agents/` 声明式权限），
其角色隔离设计有价值，但复盘发现三处盲区，红线正是从这些盲区漏出。截至 2026-09-04：

- **盲区一 · 单 CLI 覆盖**：Kiro 专属的 agent 权限只对 Kiro 会话生效，其他 CLI 会话不生效。
  红线的跨 CLI 覆盖现改由各 CLI 自动加载入口统一指向本文与 `AGENTS.md` 承担（见“跨 CLI 等效要求”）。
- **盲区二 · 动作点缺口**：文件系统权限管不住“脚本内硬编码凭据并连接数据库执行写/删”这一层，
  须由 pre-commit 凭据扫描等动作点闸门承担（见闸门 A）。
- **盲区三 · 绑定废弃流程（已消除）**：原 Kiro agent 定义绑定已停用的协作状态机；该整套机制
  （调度器、agent 定义、协作文档）已归档至 `archive/legacy-collaboration/`，不再作为活配置存在。

在这三处盲区被机制填补前，相关操作一律按“无机制保护”对待，必须由人工闸门顶上（见下）。

## 强制闸门（必须存在于动作点，而非仅在文档中）

### 闸门 A · 凭据不落地

- 源码、脚本、命令、日志、测试快照、文档与 Git 历史中不得出现明文凭据、连接串、Token 或私钥，
  包括作为“默认值”的形式（如 `os.getenv("X", "真实密码")`）。
- 凭据只能从环境变量或受控密钥机制读取，源码中不得保留任何可用的回退默认值。
- 提交前必须有机制扫描本次改动是否含凭据特征，命中即阻断提交，而不是靠人工肉眼检查。
- `.env.example` 只允许变量名与安全占位符。
- 本地 `.githooks/pre-commit` 与 CI `.github/workflows/quality.yml` 共同调用
  `scripts/project/scan_secrets.py`，分别扫描暂存区和提交范围。

### 闸门 B · 数据库与生产操作

- 数据库连接默认使用只读账号。写、DDL、导入、训练、评分、删除、模拟器启动均为写操作。
- 任何写/删操作执行前，必须先以只读方式解析精确目标与影响行数，并输出 dry-run 结果。
- 写/删操作必须满足全部条件才可执行：明确目标数据库、已核验的影响范围、可校验的恢复副本、
  验收查询、以及针对本次操作的显式授权。缺任意一项即停止。
- 授权是对“是否执行”的许可，不豁免上述任何一项执行层要求；获授权仍须备份、留痕、更新事实。
- 禁止依赖模糊环境变量目标或危险通配符定位删除对象。

### 闸门 C · 提交前一致性

- 每次提交前必须运行 `make check`（含文档提交），失败必须修复而非弱化检查。
- 若本次变更改变了事实（数据规模、架构、路径、命令、部署状态），提交必须同步更新
  `docs/CURRENT-STATE.md` 或相应当前文档；未同步的变更视为未完成。
- 数据规模类事实应改由只读查询实时派生，尽量不以手写数字留在文档中，以避免漂移。
- `.githooks/commit-msg` 与 CI 共同调用 `scripts/project/validate_commit_message.py` 校验提交主题；
  CI 同时运行 `make check`，防止本地 `--no-verify` 绕过成为最终结果。

### 闸门 D · 危险操作即停

- 遇到删除、后端/服务启停、Nginx 变更、云资源变更或 Git 发布类操作，AI 必须停止并报告，
  等待针对该操作的显式授权，不得自行推断授权。
- 生产与工作区同机：构建前端、重载服务、变更 Nginx 均直接影响生产，一律按生产变更对待，需显式授权。
- 任务书、历史提示词、规范文档本身都不构成执行授权。

## 每次任务的强制流程

无论使用哪个 CLI，一次改动必须走完以下环节；缺环节即视为未完成：

1. **加载约束**：确认 `AGENTS.md`、`docs/CURRENT-STATE.md` 与相关标准已在当前上下文中；
   若所用 CLI 无自动加载机制，必须先显式读取。
2. **记录意图**：动手前说明本次改什么、影响面、是否触碰数据库/生产/凭据、回滚方案。
3. **只读核查**：先用只读操作确认现状与目标，可逆操作优先。
4. **最小实施**：在正确领域目录内做最小连贯改动，不扩大范围。
5. **验证**：按 `TESTING-STANDARD.md` 补测试并运行检查，直至通过。
6. **回写事实**：把改变的事实写回当前文档；能派生的改为派生。
7. **交接**：报告完成项、未完成项、验证结果、剩余风险、部署状态与签名提交 ID。

## 跨 CLI 等效要求

- 每个 CLI 各自的自动加载入口（如 `AGENTS.md`、`CLAUDE.md`、`GEMINI.md`、`.kiro/steering/`）
  必须指向同一套红线；Git hooks 与 CI 在提交动作点提供 CLI 无关的等效强制。
- 新接入的 CLI 在首次使用前，必须把其自动加载入口对齐到本文与 `AGENTS.md`。
- 声明式权限规则应独立成立，不得依赖任何已停用的历史流程；原 Kiro agent 权限已随协作状态机
  一并归档至 `archive/legacy-collaboration/`。

## 与本文的关系

- 本文是执行层规范，需与内容型规范并读；内容以 `AGENTS.md` 和各现行标准为准，执行方式以本文为准。
- 本文描述的机制若尚未落地，缺口必须由人工闸门临时顶替，并明确标注为“待机制化”，不得默认已安全。
- 机制落地后，应在此更新其位置与触发点，使本文与实际闸门保持一致。
