#!/usr/bin/env bash
#
# MOD commit-message gate. Policy is declared in .gitlint.
#
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

uv run --project backend pre-commit run gitlint \
  --hook-stage commit-msg \
  --commit-msg-filename "$1"
