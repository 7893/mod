# KI-029 · 驾驶舱六屏叙事重构（导航收敛 + 屏定位调整 + 内容并入）

- 状态：DONE（2026-09-05，骨架+内容改造全部完成并主控验收）
- 更新日期：2026-09-05
- 关联链接：[已知问题看板](../KNOWN-ISSUES.md)、[ADR-0007](../decisions/0007-驾驶舱收敛为六屏叙事.md)

## 背景与目标
按 ADR-0007，将 7 候选屏收敛为 6 屏叙事，对齐观众三大关注（进度/风险/合规）+ 次线佐证，保持顶栏左右对称。
目标 6 屏：A 项目总览 / B 建设进度（含数据准备台账）/ C 上线推广 / D 风险预警 / E 合规监督 / F 业务运营。
URL 用序号路径 `/a`~`/f`，展示用中文名。

## 分工与拆分
### 轻量（主控执行）
- 导航收敛为 6 屏（左3 A/B/C + 中央标题 + 右3 D/E/F），`/data` 移出导航。
- 屏命名/定位调整：`问题清单`→`合规监督`、`智能研判`→`风险预警`（导航标签 + 页面标题）。
- URL 改序号路径 `/a`~`/f`；路由收敛；`/` 重定向到 `/a`。`/data` 路由暂留（内容合并前不删）。
- 每步 make check + 前端重建 + 线上验证；可回退。

### 重量（写 KI 派 agy，主控验收）
- B 建设进度：把 DataView（`/data` 建设推广项目台账）的台账检索/状态维护内容**并入** ConstructionView，
  内容搬不删；并入后 `/data` 路由与 DataView 方可退役（归档，遵守 ADR-0004 archive 不删）。
- E 合规监督：IssuesView 从"问题清单"转为"合规监督"视角——单位级合规概览（合规率约 92~96%、问题集中于困难户），
  可下钻到具体单位问题类型，不做逐笔会计级审计（依 BUSINESS-SIMULATION-ENGINE 第十四章）。
- D 风险预警：InsightsView 从"AI 研判"重心调为"风险预警"主场——突出"哪个单位要掉队"，与困难户/决策支撑咬合；
  展示层继续遵守 KI-023/KI-028（不展示未验证/不可信的模型指标）。

## 约束
- 单一正统命名（AGENTS.md）：新命名不带版本后缀，不保留并存变体。
- 内容并入"搬不删、保留可追溯"；退役页面走 archive 归档（ADR-0004）。
- 前端结构变更分步、每步 make check、可回退；生产与工作区同机，前端构建即影响生产（ADR-0006），按生产变更对待。

## 进度
- 轻量部分（主控完成，commit 842491c）：先落 ADR-0007 + KI-029（commit 8b8b7ee）；路由收敛为 6 屏、URL 改序号
  `/a`~`/f`（`/` 重定向 `/a`，`/data` 暂留不进导航）；导航左3（A项目总览/B建设进度/C上线推广）+ 右3
  （D风险预警/E合规监督/F业务运营），命名与图标调整（问题清单→合规监督、智能研判→风险预警），
  highRisk 徽章移到 D；修正各屏内部跳转到新路径；InsightsView 标题改"风险预警与智能研判"。
  6 屏 URL 全 200、make check 绿。
- 重量部分（agy 完成，主控验收通过，2026-09-05）：
  - 任务一（commit d4fbd9f）：DataView 台账并入 ConstructionView（数据准备台账、纳管单位、台账下钻）；
    DataView 退役归档至 `archive/legacy-frontend/DataView.vue`（搬不删，ADR-0004）；`/data` 路由已移除。
  - 任务二（commit 61942a2/c44f319）：IssuesView 转"合规监督"——合规率 92~96%、问题单位与困难户咬合、单位级合规标签，可下钻。
  - 任务三（commit a87b89d/b722894/dc187f8）：InsightsView 转"风险预警主场"——困难户/掉队高危单位，注明严守 KI-023/KI-028。
  - 主控验收：**前端规范全绿**（无私有面板样式、面板均 CockpitPanel、零手写 `<style>`、改造三屏均 ≤400 行、
    前端任意值 lint 0 命中）；6 屏线上全 200、导航对称、合规率真实计算非硬编码；make check 绿。
- 状态：DONE（2026-09-05，骨架+内容改造全部完成并验收）
