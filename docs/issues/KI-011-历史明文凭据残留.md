# KI-011 · 历史明文凭据仍存在于 archive 内容与 Git 历史中

- 状态：DONE（2026-09-05 订正关闭，风险已消解）
- 更新日期：2026-09-05
- 关联链接：[已知问题看板](../KNOWN-ISSUES.md)

- 原记录：明文口令存在于 `archive/legacy-db-scripts/` 文件与 Git 历史提交中。
- 实测核查（2026-09-05 主控）：
  - **Git 历史实际干净**：对全部提交与分支（`git grep $(git rev-list --all)`）搜索三个已知旧密码
    （`Nexus#2026`、`ModV2Reader`、`7cvfkYkx3uNIzQ`），命中数均为 0——明文从未通过被跟踪文件进入版本库，
    推送到远端的历史干净。原记录"Git 历史中仍存在"一节不成立（早期净化工程 KI-014 已处理，或这些脚本
    始终位于忽略目录）。
  - `archive/legacy-db-scripts/` 中 8 个老脚本仍含硬编码明文，但该目录被 `.gitignore` 忽略，不入库、不公开。
  - 这些均为**旧运行环境的库**凭据，该库实例已删除（迁移时确认 DELETED），密码早已失效；现行库使用不同
    admin 凭据，仅存本机 gitignored 的 `.env.systemd`。
- 关闭结论：公开的 Git 历史干净、残留明文为已失效旧库密码且仅在本地忽略目录，无实际泄露风险。按既定决策
  接受现状。若未来仍需彻底抹除本地 archive 明文，可选做（占位符替换，零风险，无实际必要）。
