# KI-100 · 大屏图表物料抽象与业务视图配置解耦

编写日期：2026-09-17
- 更新日期：2026-09-21
- 状态：DONE
- 优先级：P2
适用范围：前端六屏组件（A1~F5）、通用图表物料库、Composables 抽象层

## 问题与证据

- **关注点严重混杂**：Vue 视图层（如 `CockpitTopBar.vue`、`ConstructionView.vue`、`InsightsView.vue`、`IssuesView.vue`、`RolloutView.vue` 等）内部承载了大量长达数十行至上百行的 ECharts Option 拼装计算属性，视图组件直接依赖底层 Canvas 渲染参数。
- **公共与定制割裂**：虽然建立了 `src/charts/` 目录，但仅收拢了部分大屏面板图表；多个特定业务卡片与下钻视图仍采取内联手写方式，导致代码重复率高且修改困难。
- **状态处理与生命周期简陋**：各图表对于空数据态（Empty State）、加载骨架（Loading Skeleton）以及渲染异常缺少开箱即用的统一容器包裹，遇到无数据时只能依赖外层粗糙的 `v-if` 或绘制空轴。

## 目标与方案

1. **构建图表高阶物料层**：
   在 `frontend/src/components/charts/` 建立小型渲染原语 `ChartCanvas`，统一加载、错误、空态与 autoresize。
   不预建 `BarChartCard`、`RingGaugeCard` 等按图形命名的万能组件；只有出现稳定业务模式后才抽取对应物料。
2. **彻底解耦视图与配置**：
   业务 Vue 组件一律只负责接收数据 Props 并传递给图表组件，严禁在 `.vue` 文件中直接声明 ECharts option 字典对象。
3. **收敛全部工厂函数到 `charts/`**：
   将目前散落在 Vue SFC 内的图表配置全部抽离为纯函数（Pure Functions），并配套 Vitest 单元测试保障纯函数的输出契约。

## 修复范围与验收

- [x] 建立 `ChartCanvas` 基础图表容器，并以 A4 `OverviewTrendChart` 完成首个迁移样板。
- [x] 将 A4 时间窗与 option 构建抽为可注入时间的纯函数并补单元测试。
- [x] 将 A1 顶栏三组 option 收拢至纯函数，并统一通过 `ChartCanvas` 渲染。
- [x] 完成 B 屏图表迁移：B5 option 进入纯函数，B2/B4/B5 统一使用 `ChartCanvas`。
- [x] 完成 C 屏图表迁移：C4 option 进入纯函数，C1~C5 统一使用 `ChartCanvas`。
- [x] 完成 D 屏图表迁移：D1/D3~D7 统一使用 `ChartCanvas`，工厂补齐静态类型契约。
- [x] 完成 E 屏图表迁移：E2/E3 option 进入纯函数，E1~E4 统一使用 `ChartCanvas`，E5 台账与下钻不变。
- [x] 完成 F 屏图表迁移：F1/F3 与 F4 模型卡 option 进入纯函数，统一使用 `ChartCanvas`。
- [x] 完成 A 屏剩余图表迁移：A2/A3/A6/A8 统一画布，A5 地图 option 进入纯函数且保留专用交互外壳。
- [x] 以单一轻量 `ChartCanvas` 收口加载、错误、空态与 autoresize；不抽取高参数“万能图表”组件。
- [x] 完成共享图表积木收尾，除 `ChartCanvas` 外不再直接依赖 `vue-echarts`。
- [x] 图表业务视图不再导入 `vue-echarts` 或声明 ECharts 渲染字典；工厂具备类型契约与关键输出单测。
- [x] 确保全屏等比缩放及下钻交互行为完全不受影响，通过视觉回归测试。
- [x] `pnpm test` 与 `make check` 门禁全绿通过。

## 2026-09-20 渐进迁移约束

本 KI 不采用六屏一次性改写。每次迁移一个可独立视觉回归的面板，组件只保留模块注册、props 与工厂调用；
地图、复杂联动和业务差异明显的图表继续保留专用组件，避免为追求表面复用形成高参数万能物料。

2026-09-20 已完成 A1 顶栏渐进迁移：建设/上线双环、运营规模谱与风险闭环的配置进入
`charts/cockpitTopBarOptions.ts`，组件保留数据钳位与业务字段适配；三图继续保持各自的几何参数，
不抽取按图形命名的高参数组件。

同日完成 B 屏迁移：`ConstructionView.vue` 不再声明 ECharts option 或直接依赖 `vue-echarts`；
B5 数据准备度配置进入 `charts/constructionOptions.ts`，统一画布以 `chartClick` 透传扇区语义事件，
保留按准备状态下钻台账的交互。

C 屏随后完成迁移：`RolloutView.vue` 只保留推广数据排序与事实映射，C4 省域上线配置进入
`charts/rolloutOptions.ts`；C3 无历史快照状态改由统一画布承担，C1~C5 不再直接依赖 `vue-echarts`。

D 屏的 option 原本已集中在 `charts/operationsOptions.ts`，本轮补齐全量 `EChartsOption` 静态契约，
并将 D1、D3~D7 的六个图表统一迁入 `ChartCanvas`；双轨数据整块缺失时仍保留面板级诚实空态。

E 屏继续沿用这一边界：`IssuesView.vue` 只保留风险事实派生与下钻业务，E2 标签柱图和
E3 水位环图收拢至 `charts/complianceOptions.ts`，E1~E4 统一由 `ChartCanvas` 渲染；
标签语义配色在合规图表工厂中集中维护，E5 台账、工单抽屉与巡航交互未改动。

F 屏完成同类迁移：F1 风险总览、F3 风险维度分布及 F4 `ModelContractCard`
的小仪表收拢至 `charts/insightsOptions.ts`，三者统一使用 `ChartCanvas`；
F2 困难户解释抽屉、F5 每日简报与联动跳转未改动。`InsightsView.vue` 由 371 行降至 274 行。

A 屏剩余四个已有工厂的面板改由 `ChartCanvas` 渲染；A5 地图的比例尺、实时散点、
tooltip 与 option 进入 `charts/chinaMapOptions.ts`，地图注册、HUD 计时与省份点击仍留在专用组件。
`ChinaMap.vue` 由 481 行降至 122 行，原组件内 CSS 并入全局物料层 `styles/blocks.css`。

2026-09-21，KI-102 将 A1 收敛为无图表的五域导航摘要，原 `cockpitTopBarOptions.ts` 与对应测试随
重复累计图表一并删除；这属于信息架构退役，不回退本 KI 的图表分层原则。

同日最新提交 `bfa02e9` 的 GitHub Actions 质量门与 Deploy 作业均已成功。当前业务视图和专用图表
组件均已通过 `ChartCanvas` 渲染。

2026-09-21，最后一个共享积木 `components/blocks/CompositionBar.vue` 已改由 `ChartCanvas` 渲染，
并以组件测试锁定统一入口与精确读数展示；源码中仅 `ChartCanvas` 及验证其适配契约的测试直接依赖
`vue-echarts`。同日用户确认现行布局，两档固定场景基线更新后 22 项浏览器回归全部通过；
全屏等比缩放、下钻抽屉、窗口变化及统一画布行为保持正常，本 KI 关闭。
最终收口随提交 `d987b35` 发布为生产 release `20260921-125918`。

## 边界

- 本 KI 的物料迁移本身保持视觉与交互语义；后续由 KI-102 明确批准的信息架构和布局变化不受该约束阻止。
- 不影响现有 Pinia Store 与 API 请求契约。

## 关联

- [问题看板](../KNOWN-ISSUES.md)
- [前端规范](../development/FRONTEND-ARCHITECTURE-AND-CONSTRAINTS.md)
- [KI-099 · 图表 Design Tokens 双端契约与静态门禁治理](KI-099-图表DesignTokens双端契约与静态门禁治理.md)
