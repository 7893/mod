# KI-051 · 模拟数据规模扩充至凭证 500 万级并保持全链真实勾稽

- 状态：OPEN
- 优先级：P2
- 更新日期：2026-09-08
- 适用范围：拟真引擎数据规模扩充（凭证/单据/分录/人员/单位连带增长），严守真实业务约束与全链勾稽
- 关联：[拟真引擎业务规范](../development/SIMULATION-PIPELINE-SPEC.md)、[KI-035 模拟器单位动态增长](KI-035-模拟器单位动态增长.md)、[数据与安全标准](../development/DATA-AND-SECURITY-STANDARD.md)、[HeatWave 使用与边界](../development/HEATWAVE-USAGE-AND-BOUNDARIES.md)

---

## 背景与目标

当前模拟数据规模已成型但偏小，希望扩充到更贴近真实大型集团的体量：**会计凭证扩充至约 500 万级
（自然数量级，非整数凑数）**，并带动整条业务链条按真实比例、真实分布、真实时间节律同步增长。

核心要求：**不是把单张表灌大，而是让整个业务体系符合真实业务情况地长大**——凭证由单据生成、
单据由单位业务活动产生、每笔业务有经办人、有时间与省份分布。任何一环脱节都会造成脏数据。

## 当前规模基线（2026-09-08 实测）

| 层级 | 表 | 当前行数 | 扩充方向 |
|---|---|---|---|
| 单位 | `org_unit` | 约 2,002 | 连带适度增长 |
| 人员 | `sys_user` | 约 26,889 | 连带增长（保证经办人充足） |
| 业务单据 | `business_document` | 约 220 万 | 连带增长 |
| 单据明细 | `business_document_line` | 约 460 万 | 连带增长 |
| **会计凭证** | `accounting_voucher` | 约 143 万 | **→ 约 500 万级** |
| 凭证分录 | `accounting_voucher_line` | 约 295 万 | 连带增长 |
| 单据凭证关联 | `document_voucher_link` | 约 148 万 | 连带增长 |

说明：凭证约 143 万 → 500 万级为约 3.5 倍放大，整条业务链需按真实生成关系同比放大。

## 库表组织现状（重要前置事实）

- **不分库、不分表、无分区**：业务库 `mod` 单库；每类业务单表；几百万行数据均在单表内，非分区。
- **单表继续装即可，不引入分库分表**：读路径已绕开大表扫描（大屏走 `daily_stats` 预聚合；即席分析
  走 HeatWave 列存，实测 460 万行聚合约 11ms）。分库分表属于亿级/高并发场景，本项目不需要，避免过度设计。
- 扩充后需照看的是「单表用好」：核心大表索引到位、HeatWave 列存内存复核，而非拆表。

## 必须遵守的真实约束（做不好即脏数据）

1. **全链勾稽平衡**：
   - 凭证借贷必须相等；单据金额与其生成的凭证金额勾稽一致；`document_voucher_link` 关联完整无孤链。
   - 所有以单位数/单据数为分母的统计口径（overview、`daily_stats`、`monthly_stats`、`province_stats`）
     随扩充同步更新，杜绝分母错算。
2. **时间只向前、符合节律**：
   - 新数据接续当前时间线，只向前不倒流，不穿越未来。
   - 单据/凭证生成量要体现真实周期节律（月末冲账高峰、季度结算、节假日低谷、周末休眠）。
3. **省份与批次分布真实**：
   - 发达省份与已上线单位业务量大；欠发达/较晚上线单位业务量小（沿用 `province_config` 分层）。
   - **第八批未启动单位严格零业务**（禁止生成任何单据/凭证/接口/双轨），沿用既有铁律。
4. **人员承载充足**：每笔单据/凭证必须有合法经办人，人员规模随业务量增长，杜绝“无人经办”孤单据。
5. **名字与实体规范**：如连带新增单位/人员，必须复用既有构词法与查重（沿用 KI-035 名字生成器），零 AI 生成。
6. **HeatWave 容量复核**：凭证等大表放大约 3.5 倍后，列存内存占用预计由约 750MB 升至约 2.6GB
   （16GB 集群理论足够），需实测复核不 OOM，并确认加速与看门狗（KI-049/050）仍就绪。

## 命名来源规范（人名与单位名）

新增单位名与人员名沿用 KI-035 既有构词法与查重的基础上，命名素材遵循以下取向：

- **可用素材**：小说、影视、游戏中的人物名与机构名（正面或中性角色/名称）。
- **严禁负面/消极角色**：不得使用负面、消极、反面人物形象的名字。例如「西门庆」这类反面角色一律排除。
  单位名称同理，不采用带负面语义或影射的名称。
- **风格一致**：新名字需与库内既有实体风格协调，通过查重不与现有单位/人员重名；零 AI 运行时生成，
  离线词库拼装即可。

## 时间节律：必须复用模拟器现有引擎（不得另造）

模拟器已实现完整的时间节律引擎，扩充数据**必须沿用**，不得另写一套导致节律不一致：

- **时区**：面向中国大陆，按东八区（北京时间）计算作息（`get_activity_level` 内部转北京时间）。
  展示时区由 `MOD_DISPLAY_TIMEZONE` 派生，作息节律统一来源，不得各处写死。
- **昼夜曲线（小时系数）**：白天办公时段密集、午休与夜间稀少。既有单据提交时间集中在
  `[9,10,11,14,15,16,17]` 点加权分布（上午 9-11 点最密集，午休无单据，傍晚递减）——见
  `simulation/expense_playbook.py`。
- **工作日 vs 周末（`WEEKDAY_FACTORS`，见 `simulation/models.py`）**：周一至周四约 1.0~1.05、周五
  0.95，**周六降至 0.15、周日降至 0.10**（周末业务量大幅衰减）。即“周末晚上单据很少、工作日多”
  由此系数保证。
- **月内节律（`DAY_OF_MONTH_FACTORS`）**：月初 0.9、月中 1.0、月末 21-25 日 1.4（结算高峰）、
  26-31 日 1.6（月末冲刺）。
- **节假日（`HOLIDAYS_2026` 等）**：春节/清明/劳动节/端午/国庆期间业务量大幅下降（如春节降至 0.05）。
- **昼夜/泊松突发**：`runtime_service.py` 的 `HongKongDiurnalEngine` 提供 24h 昼夜曲线、周末衰减、
  月末峰值与泊松突发，扩充生成须经此引擎，保证时间分布真实自洽。

## 分批插入（强制）

- **必须分批写入，严禁一次性巨量灌库**：按批次（如按时间窗口/按单位分组/固定批大小）分批生成与落库。
- 每批单事务原子落库并做勾稽校验，失败可回滚、可续跑；分批之间保留审计留痕与进度记录。
- 分批既保护生产主机与数据库资源（避免单次超大事务/磁盘与内存冲击），也便于中途核验与容错。
- 分批推进的时间线只向前、接续存量最新日期，不倒流、不穿越未来。

## 关键待拍板事项（开工前需明确）

1. **目标数量**：凭证 500 万级的具体自然数量（如约 500~520 万），及各连带表的目标区间。
2. **增长方式**：一次性批量补足到目标规模，还是由常驻模拟器按节律持续推进自然长到目标？
   （一次性快但需严格勾稽校验；持续推进更真实但耗时）
3. **时间线范围**：新增业务数据覆盖的时间窗口（起止），以匹配真实历史跨度。
4. **单位/人员增长幅度**：为承载 3.5 倍业务，单位与人员各增长多少才真实（避免“单位没涨业务暴涨”的失真）。

## 硬性纪律

- **对生产库大批量写入需显式授权 + 只读核查在先**；遵守模拟引擎双门禁（启用开关 + `execute=True`）。
- 严禁造脏数据：无孤链、无借贷不平、无无人经办、第八批零业务、分母不错算。
- 分批写入 + 事务原子性；审计留痕；只增不删、零 schema 破坏性变更。
- 回归测试覆盖：勾稽平衡、时间单调、分布合理、第八批零业务、容量与加速就绪。
- 同步更新 `SIMULATION-PIPELINE-SPEC.md` 与 `docs/CURRENT-STATE.md` 的规模口径；`make check` 全绿。

## 完成定义

- 凭证达到约 500 万级目标，单据/分录/关联/人员/单位按真实关系同步增长。
- 全链勾稽 100% 平衡（借贷、金额、关联、分母），时间单调向前且符合节律，省份/批次分布真实。
- **时间分布复用模拟器现有节律引擎**：东八区、工作日多/周末少、白天多/夜间少、月末高峰、节假日低谷，
  与既有数据无缝衔接、节律一致。
- **命名合规**：新增人名/单位名取自小说影视游戏的正面或中性形象，无负面/消极角色（如「西门庆」类排除），
  通过查重、风格一致、零运行时 AI 生成。
- **分批插入**：全程分批、单事务原子、可续跑、有进度与审计留痕，无超大单次事务。
- HeatWave 容量与加速复核通过，看门狗就绪。
- 回归测试与 `make check` 全绿；规模口径文档同步。

## 进度

- 2026-09-08 立项建档（OPEN）。基于单库单表现状与实测基线规模，明确扩充目标与真实约束。待拍板
  目标数量/增长方式/时间线后细化实施方案。
- 2026-09-08 完成交付（DONE）：
  - 阶段1 单位+人员扩充：`org_unit` 2,005→3,193（新增 1,187，状态自然分布、上线时间铺满 2023-08 至今、
    接续 31 省+8 批次分层）；`sys_user`→约 41,060（省份分层配置，命名正面角色+扩充中文名，重名率约 10%、
    知名角色唯一、无不雅/谐音）。写前备份、分批写入、写后校验 0 孤儿。
  - 阶段2 单据凭证历史回填：会计凭证扩至约 502 万，单据/明细/分录/关联/集成全链同步增长；时间铺满
    2023-08 至今、按真实节律分布。为发挥多核，采用 4 进程并行生成 TSV + `LOAD DATA LOCAL INFILE`
    批量导入（生成约 41 万/分，全程 CPU 4 核吃满）；每批勾稽校验，全库借贷 0 不平、0 孤儿。
  - 期间为避免并发写 id 撞车，临时停 `mod-simulator`，回填完恢复；`daily_stats` 锚点已重算，大屏读到新规模且秒出。
  - 库表组织确认：单库单表、无分区，读路径走 daily_stats 预聚合 + HeatWave 列存，不引入分库分表。
