# KI-080 · AI 输出可信度与能力边界失真

更新日期：2026-09-13
- 状态：DONE
优先级：P1
适用范围：本地 AI 风险解释、模型状态、每日简报与 A/F 屏展示

## 问题与证据

只读审计发现：成功率零值被真值默认值替换为 100%；缺失概率被固定映射为 85%/15%；无正向归因仍制造一项 100% 权重；合成目标与单位截面切分被描述为未来预测与时序验证；简报无新鲜度判断；LLM 仅得到全国汇总却要求诊断原因；每日调用数只计成功且为进程内状态。

来源：[HeatWave 适配器](../../backend/app/integrations/heatwave_ml.py)、[特征 SQL](../../backend/app/integrations/heatwave_sql.py)、[训练评估](../../scripts/agy/train_and_evaluate_models.py)、[简报服务](../../backend/app/services/daily_briefing.py)、[AI 适配器](../../backend/app/integrations/cloudflare_ai.py)。

## 修复范围与完成定义

- [x] 零值保留；缺失/非法概率返回空值并标注来源；无正向因素时不制造归因。
- [x] API 与界面明确合成标签实验性质，不以拟合分宣布业务预测可信；保留原始评估值。
- [x] 解释数据与规则名单分工明确，前端按模型筛选补充特征，避免回归结果覆盖分类结果。
- [x] 简报按统一展示时区标记新鲜度，A/F 屏提示旧简报；读取定时刷新并处理 HTTP 失败。
- [x] LLM 限定为汇总摘要与待核实建议，过滤非法数值、拒绝异常/截断响应；修复调用计数并明确进程范围。
- [x] 相关回归测试、全量检查通过，现行文档和看板同步后关闭。

## 授权与边界

用户于 2026-09-13 明确授权新建 KI、修复可修复项并在验收后关闭。仅本地代码、文档、测试；不部署、不修改生产库、不重训、不调用外部模型。关闭表示上述缺陷已本地验收，不表示线上已生效。

未来真实预测标签、历史观测样本、前瞻评估与业务效果反馈属于后续能力建设，本 KI 不宣称完成。当前合成公式保留用于实验并明确标识；不通过修改噪声或降低门槛制造预测达标。

## 验收记录

2026-09-13 本地验收完成，关闭为 DONE：

- `make check` 成功：后端 243 项、前端 144 项、项目脚本 24 项测试通过，Ruff、样式/Token、类型检查、构建、历史完整性、文档链接与治理闸门全部通过。
- `pnpm run build:visual` 成功；`pnpm exec playwright test e2e/ai-trust.spec.ts` 在 desktop/compact 两种尺寸通过 2 项。API 全部由夹具提供，外部域名和写请求禁止；验证 A 屏旧简报入口、F 屏内容与过期提示、实验标识及面板边界，并人工检查截图。
- 新增回归覆盖：零成功率保留，缺失/非法概率，零正向贡献，展示时区跨日，合成标签不得因高分标为可信预测，模型结果覆盖，简报轮询清理与 HTTP 错误，LLM 非有限输入、异常/截断响应及失败调用计数。
- 初次全量检查仅因新 KI 状态头部格式不符合解析契约失败；已修正并重新运行全量检查成功，没有弱化闸门。
- 当前记录保留全部旧内容，以带日期的修正节明确替代旧能力表述；未修改冻结历史、未重训、未调用外部模型、未部署。

本轮完成的是输出真实性与实验边界修复。没有新增历史趋势/区域证据输入、真实未来预测、持久化摘要配额账本或业务效果反馈系统；这些仍需单独设计与数据验证，不能由本 KI 关闭推断为已实现。

