> **保全说明**：本文件为历史资料 `docs/history/22-项目目录集中整理记录.md` 的公开脱敏副本。
> **稳定 ID**：H-023
> **原件哈希 (SHA-256)**：`ca75680ca722e63e7a5e65513eecc9d0fd0632d90ad36a523d523144e719abf5`
> **脱敏规范**：遵循 [SANITIZATION-RULES.md](../development/SANITIZATION-RULES.md)，所有真实内网/公网 IP、OCID、域名及主机标识已完成安全脱敏，技术结构与演进过程 100% 保真。

---

# 项目目录集中整理记录

> 状态：历史整理记录，不代表当前目录；当前结构见 `../PROJECT-LAYOUT.md`。

整理日期：2026-08-31

## 一、整理目标

以 `/home/ubuntu/mod` 作为 JPA 唯一 MOD 项目主目录，集中代码、文档、V2 数据、参考材料、任务书及历史工作文件；Home 根目录不保留 MOD 入口，不删除历史资产，不影响 USA 运行环境。

## 二、核心路径调整

| 原Home路径 | 整理后唯一实际路径 | 当前状态 |
|---|---|---|
| Home根目录V2数据目录 | `/home/ubuntu/mod/artifacts/v2-sim-data` | 旧入口已移除 |
| Home根目录项目参考资料目录 | `/home/ubuntu/mod/references/project-source` | 旧入口已移除 |
| Home根目录V2任务书 | `/home/ubuntu/mod/docs/tasks/MOD-V2数据库整改执行提示词.txt` | 旧入口已移除 |
| Home根目录USA运维清单 | `/home/ubuntu/mod/docs/operations/MOD-USA-手工配置与验收清单.txt` | 旧入口已移除 |

## 三、工作文件归档

1. 驾驶舱需求导出源文件移入 `references/dashboard-requirement/`。
2. UI 截图、页面修复和重写脚本移入 `archive/workbench/ui/`。
3. 免费域名候选检索文件移入 `archive/workbench/domain-research/`。
4. 临时 API 文件移入 `archive/workbench/misc/`。
5. V2 生成过程中的 `fix_args.py`、`fix_gen.py`、`fix_manifest.py` 移入 `archive/v2-generation-fixes/`。

## 四、V1 工具归档

- V1 生成、导入、快照及增量程序移入 `generator/legacy-v1/`。
- V1 建库、视图、修复、账号和导入工具移入 `database/legacy-v1/`。
- 当前后端、前端和部署目录没有移动，因为 USA 正式演示仍运行 V1 链路。
- V1 数据库 `mod_s` 没有修改或删除。

## 五、未移动的外部目录

以下实验目录在首次整理盘点时仍被容器挂载，随后经用户授权停止容器并完成归档：

- DataEase 实验目录已移至 `/home/ubuntu/mod/archive/component-labs/dataease-lab`；
- NocoDB 实验目录已移至 `/home/ubuntu/mod/archive/component-labs/nocodb-lab`；
- 两个 Home 根目录旧入口均已移除。

以下目录仍在 `/home/ubuntu`，因为它们属于其他项目：
- `/home/ubuntu/modo`：其他项目，不属于 MOD。
- `/home/ubuntu/worker-wandering-rain-01aa`：其他项目，不属于 MOD。
- Lens、商旅、ADB、个人工具链及隐藏配置目录均不属于 MOD。

DataEase/NocoDB 共 6 个实验容器已经停止但未删除，实验数据完整保留。

## 六、新增导航文件

- `/home/ubuntu/mod/PROJECT-LAYOUT.md`
- `/home/ubuntu/mod/docs/INDEX.md`
- `/home/ubuntu/mod/docs/21-V2数据与项目目录现状基线.md`
- `/home/ubuntu/mod/artifacts/README.md`
- `/home/ubuntu/mod/references/README.md`
- `/home/ubuntu/mod/archive/README.md`
- 两个 `legacy-v1/README.md`

根目录 `README.md` 已更新为 V2 当前状态，不再声称 V2 尚未生成或导入。

## 七、整理后核验

- V2 数据 CSV：17 个。
- Manifest 总记录数：1,685,923。
- 17 个 CSV 重新计算 SHA-256：0 个不匹配。
- V2 任务书移动前后 SHA-256 保持一致。
- 原始项目参考文件：62 个。
- Home 根目录不再存在 MOD 兼容符号链接。
- V2 Python 生成器和只读验收工具通过语法编译检查。
- 未修改数据库、OCI、API、前端业务、Nginx、Docker或 systemd。
- 初次整理未停止服务；随后根据用户明确授权停止了 JPA 本地 6 个 DataEase/NocoDB 实验容器。USA 服务未停止或重启。
- 未删除任何项目文件。

## 八、后续注意

1. 新材料优先放入 `/home/ubuntu/mod` 对应目录，不再散落到 Home 根目录。
2. 新任务书放入 `docs/tasks/`。
3. 验收原始输出放入 `docs/evidence/`，不得包含密码。
4. V2 生成器、历史导入工具和文档均已改为项目内统一数据路径。
5. DataEase/NocoDB 已停机归档；如需恢复，应先检查 Compose 路径及绑定目录后再启动。
