> **保全说明**：本文件为历史资料 `docs/history/10-V2后续实施移交清单_给下一位AI的Prompt.md` 的公开脱敏副本。
> **稳定 ID**：H-010
> **原件哈希 (SHA-256)**：`3e3fdf5860b82ff72ff1e33a603d771ea6d84f78e188bcfd02df2a5fca023318`
> **脱敏规范**：遵循 [SANITIZATION-RULES.md](../development/SANITIZATION-RULES.md)，所有真实内网/公网 IP、OCID、域名及主机标识已完成安全脱敏，技术结构与演进过程 100% 保真。

---

# V2 模拟数据后续实施任务（移交提示词）

> 状态：历史交接提示词，已被 `../CONTRIBUTING.md` 和 `CURRENT-STATE.md` 取代，禁止作为当前指令执行。

**身份设定**：
你是 MOD (Management Operations Digitalization) 项目的高级全栈工程师代理（Agent）。

**当前上下文**：
前序 AI 已经完成了 V2 模拟数据的本地生成，所有数据文件（CSV）、结构提案（SQL）以及质量核验报告目前均安全地保存在 `/home/ubuntu/mod/artifacts/v2-sim-data` 目录中。业务方已经审阅了生成样本并予以批准。你的任务是接力完成从“数据入库”到“前后端重构”再到“生产切换”的全链路工作。

**实施前必读文档（位于 `/home/ubuntu/mod/docs/`）**：
1. `08-模拟数据V2生成与验收规范-待最终确认.md`（核心业务逻辑与规范）
2. `07-当前部署与运维基线.md`（了解 USA 服务器当前的 Docker 和 Nginx 部署情况）

**你需要执行的阶段任务**：

### 阶段一：V2 独立数据库部署与数据导入
1. **创建新库**：在现有的 MySQL 实例中，创建一个全新的独立数据库（例如 `mod_s_v2`）。**绝对禁止**修改、清空或覆盖当前的 `mod_s` 数据库，以便随时安全回退。
2. **初始化结构**：执行 `/home/ubuntu/mod/artifacts/v2-sim-data/schema-proposal.sql` 以创建表结构。
3. **数据导入**：编写 Python 脚本或使用 MySQL Load Data 命令，将同目录下的所有 `.csv` 数据导入到对应的表内。

### 阶段二：后端 API 重构（FastAPI）
1. **修改数据源**：将后端的环境变量或配置指向新的 `mod_s_v2` 数据库。
2. **接口重写**：根据 V2 的新表结构（`org_unit`, `rollout_batch`, `business_document`, `accounting_voucher` 等）重写读取逻辑。
3. **满足六屏需求**：确保 API 能够输出符合规范要求的 6 大主屏数据：总览、建设、推广、业务、问题、智能研判，并提供完整的聚合运算逻辑。
4. **增加下钻接口**：编写提供给单位详情“下钻（Drill-down）”所需的明细查询接口。

### 阶段三：前端页面重构（Vue 3）
1. **对齐数据结构**：调整前端项目 `/home/ubuntu/mod/frontend` 中的 Pinia Store（如 `project.ts`），适配新的 V2 API JSON 响应字段。
2. **展示深度细节**：将目前前端纯视觉的“生命周期大屏”与深度的 V2 数据（各类异常率、单据凭证、双轨一致性等）全方位打通。
3. **下钻与异常态**：实现单位层级的详情下钻页面，以及应对数据缺失情况下的完美空状态（Empty States）处理。

### 阶段四：生产切换与验证
1. **重构部署**：通过 `rsync` 等工具将修改后的前后端代码同步至部署环境（`usa` 主机）。
2. **容器更新**：在 `usa` 环境重启后端的 Docker 容器服务（`docker-compose up -d --build`），并构建前端 Vue 代码。
3. **验证与日志**：确认应用能够在新架构下正常启动且数据无缝衔接。

**底线要求**：
- 每完成一个阶段，务必运行相关的本地校验。
- 绝不可破坏或导致现有 V1 (`mod_s`) 环境宕机。保留 V1 随时切回的能力。
- 请采用结构化、严谨的工程步骤来完成，并在结束时输出完整的部署确认报告。
