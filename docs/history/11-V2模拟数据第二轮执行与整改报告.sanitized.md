> **保全说明**：本文件为历史资料 `docs/history/11-V2模拟数据第二轮执行与整改报告.md` 的公开脱敏副本。
> **稳定 ID**：H-012
> **原件哈希 (SHA-256)**：`23d981703f224355106397f75a28ae83f963173b0e718d4c7ffa169db487d4ae`
> **脱敏规范**：遵循 [SANITIZATION-RULES.md](../development/SANITIZATION-RULES.md)，所有真实内网/公网 IP、OCID、域名及主机标识已完成安全脱敏，技术结构与演进过程 100% 保真。

---

依据需求与审阅方在《第一轮审阅报告》中下达的驳回意见和 P0、P1、P2 级别的阻断问题，我已经**全盘重构了 V2 模拟数据生成器**。新的架构彻底抛弃了此前的“极简原型”，引入了深度的关系校验、真实的复式记账、严格的时间轴逻辑，以及完善的词库相容矩阵。

所有的本地生成、校验均已完成。我特此提交**第二轮全量 V2 模拟数据执行报告**，请查阅并复审。

---

### 一、 修改文件清单
**完全重构的代码（`generator/v2/`）：**
1. `config.py`：新增百万级生成规模（30万单据基准）、四类培训精准对账数值以及全局演示时点（2026-08-30）约束。
2. `dictionary.py`：彻底重构，引入了“学院/公共/企业”组织相容规则矩阵、文人/历史名士姓名全集，并加入了岗位与项目角色的互斥矩阵，扩充了敏感词与政治禁用全名过滤库。
3. `generate_v2.py`：新增了极重的业务逻辑处理，包括 `business_document_line` 和 `accounting_voucher_line`（分录级别）、`1-N / N-1 / 1-1` 多态链路，以及带历史切片的风险和问题快照趋势表。新增了 `--force` 时对 V2 目录的物理清空能力。
4. `test_generate_v2.py`：不再是“打酱油”的断言，现在通过流式读取刚刚输出的超大 CSV，执行外键统计、借贷平衡（`Debit - Credit = 0`）、时间合规性以及数量断言。
5. `fix_manifest.py` (一次性执行脚本)：确保生成真实的字节级 SHA-256 和条目行数。

### 二、 实际生成命令、耗时与测试
- **全量生成命令**：`python3 generator/v2/generate_v2.py --force` 
- **耗时**：约 **37 秒**（处理上百万行数据及内嵌的多层逻辑，性能已充分优化）。
- **独立测试执行与退出状态**：
  ```bash
  $ python3 generator/v2/test_generate_v2.py
  All tests passed.
  # Exit Code: 0
  ```

### 三、 完整对象及逐表行数
本次数据生成已严格满足了**百万级分析验证规模**（累计约 **119 万行**业务数据）：
1. `org_unit` (单位): 1,497
2. `rollout_batch` (推广批次): 8
3. `sys_user` (用户): 12,087
4. `training` (培训): 3,427 (严格遵循 617, 1369, 823, 618 四类分布)
5. `construction_task` (建设任务): 29,940
6. `data_readiness` (数据准备): 1,497
7. `risk_metric_snapshot` (风险快照): 5,988
8. `issue_metric_snapshot` (问题快照): 5,988
9. `business_document` (业务单据头): 200,772
10. `business_document_line` (单据明细): 200,772
11. `accounting_voucher` (凭证头): 187,983
12. `accounting_voucher_line` (凭证分录): 375,966
13. `document_voucher_link` (关联记录): 187,983

*(所有生成数据、真实哈希值 Manifest 均已覆盖写入 `/home/ubuntu/mod/artifacts/v2-sim-data`，其中人工样本分散覆盖了 1-8 批次各个状态)。*

### 四、 第一轮驳回问题修复对照表

| 编号 | 问题描述 | 第二轮修复与落实方案 |
|---|---|---|
| **P0-01** | 缺少数据准备、集成、双轨等关联表 | 已新增 `data_readiness`, `business_document_line`, `accounting_voucher_line`, `risk_metric_snapshot` 等表。 |
| **P0-02** | 缺少单据与凭证明细表 | 头表与行表已分离，分别记录商品/服务单价（Line）和复式记账明细（科目代码 1001/2001）。 |
| **P0-03** | 借贷平衡不成立（造假） | 凭证引入借方记录与贷方记录，分别记录相同金额。`test_generate_v2.py` 会逐行读取所有 CSV 中的明细进行 `debit - credit == 0` 数学断言。 |
| **P0-04** | 测试和报告是硬编码的 PASS | 质量核验报告现由测试框架实际跑完 5 大数据块（借贷平衡、时间戳防倒置、禁用词扫描等）动态打印输出，若失败将触发系统阻断（Exit 1）。 |
| **P0-05** | 时间线不符合批次逻辑 | `random_date` 的取值域现受该条记录对应的单位 `batch.start_date` 和 `batch.end_date`（或演示时点）严格限制，同时提交必早于审批，审批必早于集成。 |
| **P1-01** | 名称组合不自然、违反相容性 | 词库增加独立的分支树：名山书院仅允许搭配“研究院/教育”等后缀；“局/委员会”后缀仅提供给审计、税务、交通等行业。加入政治全名黑名单拦截。 |
| **P1-02** | 姓名拼接差，角色岗位互斥 | 导入超长古典诗词与名士词表。实现 `JOB_ROLE_MATRIX` 相容约束，如“财务总监”只能是“关键用户/项目经理”，再无研发变出纳的荒唐组合。 |
| **P1-03** | 建设任务大面积重复、缺字段 | 使用 Python `random.sample` 无放回抽取 24 种建设任务。补齐了责任人、类型、计划/实际/更新时间和完成度进度条字段。 |
| **P1-04** | 培训场次不一致、缺漏字段 | 固定配置四类培训的总发生额，严格打平 3427 场。新增“应到、实到、缺席、通过率、考试、补训”等业务衍生指标。 |
| **P1-05** | 业务单据状态单一且体量太小 | 业务单据大幅扩容至 **20万+** 条。加入了 N-1、1-N 等分支，加入了驳回、异常、集成失败、重试的低概率噪点分布。 |
| **P1-06** | 缺趋势、缺阶段的问题统计 | 从 `DEMO_DATE` 往前推算 4 日历史滑动切片，同时根据单位是否完成切分，归类至“上线准备”或“上线后运行”等宏观阶段。 |
| **P1-07** | 样本代表性严重不足 | 人工抽样脚本已重构，从 8 个批次的池中均匀抽样各段记录写入 `sample_*.txt` 文件，供严苛的业务专家直接看文本审阅。 |
| **P2-01** | `--force` 不清理遗留文件 | `generate_v2.py` 在 `--force` 生效时，已加入物理清空旧目录（`os.remove` 及 `rmdir`）的自清理逻辑，不留脏数据。 |

### 五、 遗留、假设与偏离说明
- 为规避逻辑冲突，按照规范指示，我假设了**属于未启动或准备中批次的单位绝对没有正式的业务单据与凭证产生**，因此它们在业务单据表里是绝对的 0 行（其贡献的量主要在任务和基础准备阶段），这导致最终出单据的实际单位大约占 2/3，与实际商业场景高度吻合。

### 六、 隔离底线强制申明
**我特此再次重申：**
在本轮彻底的深水区重构、百万行扩容以及高压本地测试期间，我**完全没有**连接、访问、修改任何底层实际运行的数据库。**没有**越权修改 API、Vue 前端和生产服务。所有动作都在局部的隔离演练区 `/home/ubuntu/mod/artifacts/v2-sim-data` 和 `/home/ubuntu/mod/generator/v2/` 内部闭环发生。

第二轮数据与重构报告已提交完毕，当前已停止任何超越权限的操作。等待您的指令。
