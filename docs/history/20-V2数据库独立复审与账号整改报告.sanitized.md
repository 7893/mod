> **保全说明**：本文件为历史资料 `docs/history/20-V2数据库独立复审与账号整改报告.md` 的公开脱敏副本。
> **稳定 ID**：H-021
> **原件哈希 (SHA-256)**：`1ec1170aa51a321daa325a1499e1583fc59933db9f26928d1ac21a489c45d5eb`
> **脱敏规范**：遵循 [SANITIZATION-RULES.md](../development/SANITIZATION-RULES.md)，所有真实内网/公网 IP、OCID、域名及主机标识已完成安全脱敏，技术结构与演进过程 100% 保真。

---

# V2 数据库独立复审与账号整改报告

**整改时点**: 2026-08-31 09:46:04 (Asia/Shanghai)  
**执行角色/模型**: Antigravity (Google DeepMind)  
**验收工具**: `/home/ubuntu/mod/database/verify_mod_s_v2_readonly.py`  
**工具 SHA-256**: `03d86f786268c205344e22d17f56240b311678333b4278f29f12c86730d3687c`  
**整改与复审性质**: 100% 独立只读验收（除账号定点安全修正外，0业务数据改动，0结构调整）

---

### 一、 数据库账号安全定点整改

依据任务书要求，修复了初版授权中因主机名通配不当导致的异常账号状态，实现了最小权限精确 Host 绑定：

#### 1. 整改前后账号状态对比表

| 账号标识 | 整改前状态与权限 | 整改后状态与权限 | 处置说明 |
|---|---|---|---|
| `mod_v2_reader@'10.0.0.x (USA宿主机私网地址，已脱敏)'` | **不存在** | **`ACTIVE` (locked='N')**<br>仅 `GRANT SELECT ON mod_s_v2.*` | **新建正式 V2 只读账号**，精准绑定 USA 主机私网 IP |
| `mod_v2_reader@''` | `ACTIVE` (locked='N')<br>拥有 `SELECT ON mod_s_v2.*` | **`LOCKED` (locked='Y')**<br>仅 `GRANT USAGE ON *.*` | **收回 mod_s_v2 权限并锁定账号**（未 DROP） |
| `mod_v2_reader@'%'` | `ACTIVE` (locked='N')<br>仅 `GRANT USAGE ON *.*` | **`LOCKED` (locked='Y')**<br>仅 `GRANT USAGE ON *.*` | **直接锁定账号**（未 DROP） |

#### 2. 实际执行的账号修复 SQL（密码已脱敏）
```sql
-- 1. 创建精确绑定 USA 宿主机 IP 的专用只读用户并授权
CREATE USER 'mod_v2_reader'@'10.0.0.x (USA宿主机私网地址，已脱敏)' IDENTIFIED BY '<REDACTED>';
GRANT SELECT ON mod_s_v2.* TO 'mod_v2_reader'@'10.0.0.x (USA宿主机私网地址，已脱敏)';

-- 2. 收回异常宽泛账号权限并锁定
REVOKE SELECT ON mod_s_v2.* FROM 'mod_v2_reader'@'';
ALTER USER 'mod_v2_reader'@'' ACCOUNT LOCK;
ALTER USER 'mod_v2_reader'@'%' ACCOUNT LOCK;
```

#### 3. 凭据存储规范
- 凭据文件位置：仅保存在 USA 宿主机 `/home/ubuntu/mod/.env.v2`。
- 文件权限：`chmod 600`，所有者为 `ubuntu`。
- 安全策略：已将 `.env.v2` 加入 `.gitignore`，严禁打印或提交密码。

---

### 二、 独立只读验收工具设计与安全规范

编写了与导入工具完全解耦的独立验证程序 `/home/ubuntu/mod/database/verify_mod_s_v2_readonly.py`：
1. **零特权依赖**：源码不含任何管理员凭据，仅通过 `mod_v2_reader` 运行。
2. **纯只读指令集**：源码仅包含 `SELECT`、`SHOW` 和 `EXPLAIN`，无任何写操作。
3. **断言无硬编码**：所有指标、外键与逻辑等式均由底层数据动态聚合比对，任何断言失败立即抛出非零退出码。

**脱敏执行命令**:
```bash
ssh usa "python3 /home/ubuntu/mod/database/verify_mod_s_v2_readonly.py"
```
**实际执行耗时**: **6.42 秒**  
**退出码 (Exit Code)**: **`0` (ALL 25 ASSERTIONS PASSED)**

---

### 三、 独立复审全量验收断言结果清单

#### 1. 安全权限与环境配置断言
| 序号 | 检查项 | 预期指标 | 实际执行结果 | 判定 |
|:---:|---|---|---|:---:|
| 01 | 当前执行账号确认 | `mod_v2_reader@10.0.0.x (USA宿主机私网地址，已脱敏)` | `mod_v2_reader@<usa-vm-instance-id> (auth: mod_v2_reader@10.0.0.x (USA宿主机私网地址，已脱敏))` | **PASS** |
| 02 | 最小只读权限边界校验 | 仅 `SELECT ON mod_s_v2.*` | `2 条Grants (只读)` | **PASS** |
| 03 | 会话时区显式对齐 | `+08:00` | `+08:00` | **PASS** |

#### 2. Schema 与对象隔离断言
| 序号 | 检查项 | 预期指标 | 实际执行结果 | 判定 |
|:---:|---|---|---|:---:|
| 04 | 数据库字符集与排序规则 | `utf8mb4 / utf8mb4_unicode_ci` | `utf8mb4 / utf8mb4_unicode_ci` | **PASS** |
| 05 | 数据表数量与类型 | 恰好 17 张 `BASE TABLE` | 17 张基础表 | **PASS** |
| 06 | 视图与对象隔离 | 0 视图 | 0 个视图 | **PASS** |
| 07 | 触发器/例程/事件对象隔离 | 全部为 0 | 触发器=0, 存储过程=0, 事件=0 | **PASS** |

#### 3. 逐表记录行数核验（总计 1,685,923 行）
| 序号 | 检查项 | 预期指标 | 实际执行结果 | 判定 |
|:---:|---|---|---|:---:|
| 08 | 17 张表逐表行数精确匹配 | 0 表差异 | 0 表差异 (17/17 匹配) | **PASS** |
| 09 | 全量数据集总记录数 | 1,685,923 行 | 1,685,923 行 | **PASS** |

逐表行数明细：
- `rollout_batch`: 8 行
- `org_unit`: 1,497 行
- `sys_user`: 12,087 行
- `training`: 3,427 行
- `construction_task`: 44,877 行
- `business_document`: 199,612 行
- `business_document_line`: 498,855 行
- `accounting_voucher`: 161,270 行
- `accounting_voucher_line`: 322,540 行
- `document_voucher_link`: 182,201 行
- `integration_result`: 150,233 行
- `dual_run_result`: 2,859 行
- `data_readiness`: 1,497 行
- `rollout_status_snapshot`: 74,850 行
- `issue_metric_snapshot`: 15,030 行
- `risk_metric_snapshot`: 15,030 行
- `metric_snapshot`: 50 行

#### 4. 全量 20 条真实外键完整性核验（0 孤立记录）
| 序号 | 检查项 | 预期指标 | 实际执行结果 | 判定 |
|:---:|---|---|---|:---:|
| 10 | 真实外键约束总数核验 | 精确 20 条 | 20 条外键（动态读取自 information_schema） | **PASS** |
| 11 | 20 条外键孤儿记录全量核验 | 孤立记录数 0 | 孤立数 0（全部 20 条外键孤儿数均为 0） | **PASS** |

20 条真实外键关系与检测明细：
1. `org_unit.batch_id -> rollout_batch.id`: 0 孤立
2. `sys_user.org_id -> org_unit.id`: 0 孤立
3. `training.org_id -> org_unit.id`: 0 孤立
4. `training.batch_id -> rollout_batch.id`: 0 孤立
5. `construction_task.org_id -> org_unit.id`: 0 孤立
6. `business_document.org_id -> org_unit.id`: 0 孤立
7. `business_document_line.doc_id -> business_document.id`: 0 孤立
8. `accounting_voucher.org_id -> org_unit.id`: 0 孤立
9. `accounting_voucher_line.voucher_id -> accounting_voucher.id`: 0 孤立
10. `document_voucher_link.doc_id -> business_document.id`: 0 孤立
11. `document_voucher_link.voucher_id -> accounting_voucher.id`: 0 孤立
12. `integration_result.voucher_id -> accounting_voucher.id`: 0 孤立
13. `dual_run_result.org_id -> org_unit.id`: 0 孤立
14. `data_readiness.org_id -> org_unit.id`: 0 孤立
15. `data_readiness.batch_id -> rollout_batch.id`: 0 孤立
16. `rollout_status_snapshot.org_id -> org_unit.id`: 0 孤立
17. `issue_metric_snapshot.org_id -> org_unit.id`: 0 孤立
18. `issue_metric_snapshot.batch_id -> rollout_batch.id`: 0 孤立
19. `risk_metric_snapshot.org_id -> org_unit.id`: 0 孤立
20. `risk_metric_snapshot.batch_id -> rollout_batch.id`: 0 孤立

#### 5. 金额与业务逻辑断言
| 序号 | 检查项 | 预期指标 | 实际执行结果 | 判定 |
|:---:|---|---|---|:---:|
| 12 | 凭证分录复式记账借贷平衡 | 不平数 0 | 不平数 0 | **PASS** |
| 13 | 凭证分录完整性 (每证>=1条) | 空凭证数 0 | 空凭证数 0 | **PASS** |
| 14 | 单据明细汇总金额与主表强平 | 不平数 0 | 不平数 0 | **PASS** |
| 15 | 单据明细完整性 (每单>=1条) | 空单据数 0 | 空单据数 0 | **PASS** |
| 16 | 双轨结论与金额数学差值一致性 | 矛盾数 0 | 数学差异=0, 逻辑冲突=0 | **PASS** |
| 17 | 数据准备动态同步项数等式 | 矛盾数 0 | 矛盾数 0 (成功+失败+待处理=总数) | **PASS** |
| 18 | 问题快照分类与解决数等式 | 矛盾数 0 | 矛盾数 0 | **PASS** |
| 19 | 单据/凭证/集成状态枚举合法性 | 非法状态 0 | 非法状态 0 | **PASS** |

#### 6. 日期防伪与时间边界断言
| 序号 | 检查项 | 预期指标 | 实际执行结果 | 判定 |
|:---:|---|---|---|:---:|
| 20 | 实际业务与快照不晚于基准日 | 未来记录 0 | 覆盖10大涉时字段，未来记录数 0 | **PASS** |

#### 7. 终态 KPI 动态底层重算 vs 快照比对断言
| 序号 | 检查项 | 预期指标 | 实际重算与快照比对结果 | 判定 |
|:---:|---|---|---|:---:|
| 21 | 8月30日上线/稳定单位数 | 748 家 | 底层重算=748 家，快照=748 家 (100%一致) | **PASS** |
| 22 | 8月30日双轨运行单位数 | 205 家 | 底层重算=205 家，快照=205 家 (100%一致) | **PASS** |
| 23 | 终态凭证生成成功率 | 96.51% | 单据重算=96.51% (165,417/171,397)，快照=96.51% | **PASS** |
| 24 | 终态接口集成成功率 | 94.67% | 集成重算=94.67% (142,226/150,233)，快照=94.67% | **PASS** |

#### 8. 大屏专用复合索引与 EXPLAIN 计划分析
| 序号 | 检查项 | 预期指标 | 实际执行结果 | 判定 |
|:---:|---|---|---|:---:|
| 25 | 复合索引数量与执行计划 | >= 10 组索引 | 18 组二级索引存在，核心查询均命中索引 | **PASS** |

**核心查询 EXPLAIN 执行计划摘要**:
- **单据大屏多维查询**: `business_document` -> `type=range`, `key=idx_doc_org_nature_status_time`, `rows=235`
- **凭证台账状态时间查询**: `accounting_voucher` -> `type=range`, `key=idx_voucher_org_status_time`, `rows=201`
- **接口流水按凭证下钻**: `integration_result` -> `type=ref`, `key=idx_integ_voucher_status_time`, `rows=1`
- **推广快照日期与状态统计**: `rollout_status_snapshot` -> `type=ref`, `key=idx_snap_date_status_org`, `rows=446`
- **单位批次状态地区筛选**: `org_unit` -> `type=ref`, `key=idx_org_batch_status_region`, `rows=2`

---

### 四、 USA 宿主机真实连接与权限隔离复测

在 USA 宿主机环境上使用 `mod_v2_reader` 实际执行探测：
1. `SELECT COUNT(*) FROM mod_s_v2.org_unit` $\rightarrow$ **1,497 行 (执行成功)**。
2. `SELECT COUNT(*) FROM mod_s_v2.business_document` $\rightarrow$ **199,612 行 (执行成功)**。
3. `SELECT COUNT(*) FROM mod_s.org_unit` $\rightarrow$ **`OperationalError: 1142 SELECT command denied` (权限强隔离生效，访问被拒绝)**。

---

### 五、 遗留风险声明

- **管理员凭据风险说明**：依据用户当前明确决策，暂时不轮换 MySQL 主管理员密码。现有历史脚本或配置中保存的管理员凭据风险依然存在，后续应在专项安全运维阶段实施统一密码轮换与集中安全管理。

---

### 六、 安全与停止点声明

我方在此郑重声明：
- 本轮整改严格限定在 **账号权限修正、独立只读验收程序编写及文档更正** 范围内；
- **未执行任何业务数据变更（0 INSERT/UPDATE/DELETE/REPLACE/TRUNCATE/REIMPORT）**；
- **未改动任何表结构及索引（0 CREATE/ALTER/DROP TABLE）**；
- **未对现行生产库 `mod_s` 产生任何影响**；
- **未修改 API 代码、前端代码、Nginx 规则、Docker 容器或生产配置**；
- **未输出或泄露任何密码明文**。

工作已全面完成，正式停止操作，等待需求与审阅方独立复核！
