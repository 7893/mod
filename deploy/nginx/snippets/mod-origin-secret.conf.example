# CloudFront 回源密钥校验片段模板 (KI-078)
# 生产环境由运维独立部署至 /etc/nginx/snippets/mod-origin-secret.conf，权限设置为 0600
# 严禁将真实密钥文件纳入 Git 版本跟踪
if ($http_x_origin_secret != "__CLOUDFRONT_ORIGIN_SECRET__") {
    return 403;
}
